package interaction.trigger;

/**
 * Provides auxiliary constants that represent reasons for postponing the interaction.
 *
 * @author MTomczyk
 */
public enum Reason
{
    /**
     * Could not determine the reference sets (there are no alternatives for evaluation by the decision maker)
     */
    COULD_NOT_DETERMINE_REFERENCE_SETS
}
