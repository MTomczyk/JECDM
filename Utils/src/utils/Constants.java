package utils;

/**
 * Some common fields (constants) are stored here.
 *
 * @author MTomczyk
 */


public class Constants
{
    /**
     * Default, small value that can be used, e.g., for comparing doubles.
     */
    public static final double EPSILON = Math.pow(0.1d, 14.0d);
}
